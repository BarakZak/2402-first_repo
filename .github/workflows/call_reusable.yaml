name: Call Reusable Workflow
on:
  push:
    branches:
      - main
      - master

jobs:
  use-reusable:
    uses: BarakZak/2402-first_repo/.github/workflows/reusable-test.yaml@master
    with:
      cool_var: "amazing"
